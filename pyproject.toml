[project]
name = "kanade"
version = "0.2.0"
description = "Apple Music Helper"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bullmq>=2.0.0",
    "flask>=3.0.0",
    "flask-cors>=5.0.0",
    "gamdl>=3.7.3",
    "gunicorn>=23.0.0",
    "psycopg[binary]>=3.2.0",
]

[dependency-groups]
dev = ["pytest>=9.0.2"]
